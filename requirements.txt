# Core: reinforcement learning + numerics.
# These reproduce every figure, table, and number of Blocks 1-3 without BOPTEST
# ("Level B" in REPRODUCE.md). Python 3.11 is the reference interpreter.
stable-baselines3==2.1.0
torch
gymnasium
numpy<2.0.0
pandas
matplotlib
pyyaml>=6.0
requests            # BOPTEST RTE HTTP client (any live-simulator evaluation)

# Optional: experiment logging (training only; not needed to reproduce results).
tensorboard
wandb

# Optional: auxiliary scripts only (NOT needed to reproduce any result).
#   sinergym       # alternative non-BOPTEST backend (envs/backends/sinergym_backend.py)
#   python-docx    # Word-artifact organizer (evaluation/organize_paper_artifacts.py)

# BOPTEST is a separate open-source project and is NOT installed via pip.
# Run it from https://github.com/ibpsa/project1-boptest (Docker recommended)
# and point the environments in envs/ and configs/ at your local runtime.
